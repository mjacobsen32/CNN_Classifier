class Args:
    desired_length = int
    subset_indices = int
    train_indices = int
    test_indices = int
    train_length = int
    test_length = int
    batch_size = int
    test_batch_size = int
    epochs = int
    lr = float
    gamma = float
    log_interval = int
    num_classes = int
    optimizer = str
    model = str
    percent_data = int
    loss = str
    input_dimension = int
    lr_sched = str
    save_model = bool
    no_cuda = bool
    dry_run = bool
    seed = int
    augmentations = []
    class_weights = []
    output_file_name = str
    tb_dir = str
