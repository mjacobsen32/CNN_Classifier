import logging
import constants.scatterPlotConstants as constants

modelLog = logging.getLogger('create_model')
modelLog.setLevel(logging.INFO)

formatter = logging.Formatter('%(levelname)s: %(message)s')